public class GameLogic {
    public static void main(String[] args) {
        // 游戏逻辑
        /*
            读取一个signal
            if(是否是界面）{
                1. 关闭游戏
                2. 重启
                3. 返回上一步
                4. 保存游戏
            }else{
                if(是否是选定状){
                    if(是否是同一棋子||空白位置){
                        重置选定状态
                        update
                    }
                    else{
                        吃子或者移动
                        位置更新
                        重置选定状态
                        update
                    }
                }
                else{
                    选择棋子（判定能走的位置）
                    更新选定状态（更新当前选定棋子，能走的位置，能吃的棋子）
                    update
                }


            }
         */
    }
}
